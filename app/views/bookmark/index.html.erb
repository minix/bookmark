<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>List Bookmark</title>
    <style>
      body, h1, h2, h3, a, ol, li, ul, span, time, p{ margin:0px; padding:0px; } 
      div#main_content{ width:968px; position:relative; margin:0 auto; border:1px solid #E4E4E4; }
      div#b_list{ float:left; width:690px; margin-right:8px;  }
      div#b_list .nav_list{ list-style:none; border:1px solid #dedeaf; border-radius:5px; -webkit-border-radius:5px; -moz-border-radius:5px; margin:0; padding:10px; } 
      div#b_list .nav_list li{ margin:0px; padding:10px 55px 10px 0px; position:relative; border-bottom:1px solid #dedeaf; border-top:1px solid #fff; }
      div#b_list .nav_list li:first-child{ padding-top:0; border-top:none; }
      div#b_list .nav_list li h3{ margin-bottom:5px; }
      div#b_list .nav_list li time{ font-size:0.7em; color:#E4E4E4; }
      div#b_list .nav_list li .search{ font-size:0.8em; color:#FFF; border-radius:2px; margin-top:5px; -moz-border-radius:2px; -webkit-border-radius:2px; background-color:#88C6ED; padding:2px 4px; }
      div#b_list .nav_list li a.content{ text-decoration:none; color:#007FC7; }
      div#b_list .nav_list li a.content:hover{ color:#00488A; }
      div#b_list .nav_list li p.abc{ float:right; }
      div#b_list .nav_list li p.abc span{ border:1px solid #E4E4E4; margin-bottom:10px; margin-right:1px; }
      div#b_list .nav_list li p.abc span a{ color:#E4E4E4; font-size:.8em; padding:3px; }
      div#b_list .nav_list li p.abc span a:hover{ background-color:#007FC7; color:#FFF; }

      div#b_tag{ float:left;width:260px;  border:1px solid #E4E4E4; position:relative; }
      div#b_tag .nav_tag{ list-style:none; } 
      div#b_tag .nav_tag li{ display:block; }
      div#b_tag .nav_tag li a{ }
    </style>
  </head>
  <body>
    <div id="main_content">
      <%= link_to "Save Bookmark", :controller => "bookmark", :action => "new" %>
      <div id="b_list">
        <h2>Current Bookmark</h2> 
        <ol class="nav_list">
          <% @bookmark.each do |bookmark| %>
            <li>
              <h3>
                <a class="content" href="<%= bookmark.url %>"><%= bookmark.state %></a>
              </h3>
              <p> 
                <span class="search"><%= bookmark.tag %></span> <time><%= bookmark.created_at.strftime('%Y-%m-%d') %></time>
              </p>
              <p class="abc">
                <span><%= link_to "Delete", :action => "destroy", :id => bookmark, :confirm => "Are you Sure", :method => :delete %></span>
                <span><%= link_to "Edit", :action => "modify", :id => bookmark %></span>
              </p>
            </li>
          <% end %>
        </ol>
      </div>
      <div id="b_tag">
        <h2>Tag</h2>
        <ul class="nav_tag">
          <% @tag.each do |tab| %>
            <li><a href="<%= tab.tag %>"><%= tab.tag %></a></li>
          <% end %>
        </ul>
      </div>
    </div>
  </body>
</html>
