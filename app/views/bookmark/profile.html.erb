<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Persion Profile</title>
  </head>
  <body>
    Welcome, <%= @user.name %>
    <h2>Yor like bookmark</h2>
      <ul>
        <% @url.each do |url| %>
          <li><a href="<%= url.url %>"><%= url.url %></a>
          <%= link_to "Delete", :action => "destroy", :id => url, :confirm => "Are you Sure", :method => :delete %></span>
          <%= link_to "Edit", :action => "modify", :id => url %></span>
          <% end %>
          </li>
      </ul>
  </body>
</html>
