<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Login</title>
    <meta charset="utf-8" />
    <style>
      h1, form, label, a, br, body, fieldset, input{ margin:0px; padding:0px; } 
      body{ color:#333; font-size:normal; }
      a{ text-decoration:none; font-size:.8em; }
      div#main{ width:500px; position:relative; margin:0 auto; top:100px; }
      div#main .info{ font-size:2em; color:#3E3E3E; border-bottom:2px solid #3E3E3E; margin-bottom:12px; padding-bottom:10px; }
      .glob{ margin:1px 0px 8px 1px; }
      .glob:focus{ background-color:#FDFAC3; }
      .glob label{ font-size:.8em; }
      .glob input{ width:350px; height:30px; font-size:1.2em; border:1px solid #121735; }
      .glob input:focus{ border:1px solid #333; }
      .glob input:hover{ }
      .submit{  }
      .submit input{ width:80px; height:30px; font-size:1em; }
    </style>
  </head>
  <body>
    <div id="main">
      <%= form_for :user, :url => { :controller => "user", :action => "login"} do |f| %>
		    <p class="info">Login</p>
        <em style="font-size:.8em; ">Please Login to share your like bookmark</em>
        <div class="glob">
		      <label for="user_login">Mail</label><br />
		      <%= f.text_field :name, :size => 20 %>
		    </div>
        <div class="glob">
		      <label for="user_password">Password</label><br />
          <%= f.password_field :password, :size => 20 %>
        </div>
        <div class="submit">
          <%= submit_tag "Submit" %><br />
        </div>
		    <%= link_to 'Register', :action => 'signup' %>
		    <%= link_to 'Forgot my password', :action => 'forgot_password' %>
      <% end %>
    </div>
  </body>
</html>
